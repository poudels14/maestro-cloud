{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "allOf": [
    {
      "if": {
        "not": {
          "required": ["$extends"]
        }
      },
      "then": {
        "required": ["cluster", "ingress"],
        "oneOf": [{ "required": ["encryption-key"] }, { "required": ["encryptionKey"] }],
        "properties": {
          "cluster": {
            "required": ["name"]
          },
          "tailscale": {
            "oneOf": [{ "required": ["auth-key"] }, { "required": ["authKey"] }]
          },
          "datadog": {
            "oneOf": [{ "required": ["api-key"] }, { "required": ["apiKey"] }]
          },
          "cloudflare": {
            "required": ["tunnel"],
            "properties": {
              "tunnel": {
                "required": ["token"]
              }
            }
          },
          "slack": {
            "oneOf": [{ "required": ["webhook-url"] }, { "required": ["webhookUrl"] }]
          },
          "log-backup": {
            "required": ["bucket"],
            "oneOf": [{ "required": ["kms-key-id"] }, { "required": ["kmsKeyId"] }]
          },
          "logBackup": {
            "required": ["bucket"],
            "oneOf": [{ "required": ["kms-key-id"] }, { "required": ["kmsKeyId"] }]
          }
        }
      }
    }
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "$extends": {
      "type": "string",
      "minLength": 1,
      "description": "Base config source. Objects merge recursively; arrays and scalar values in this config replace inherited values."
    },
    "cluster": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        },
        "nodes": {
          "type": "array",
          "items": {
            "type": "string",
            "anyOf": [
              {
                "format": "ipv4"
              },
              {
                "pattern": "^[0-9]{1,3}(\\.[0-9]{1,3}){3}:[0-9]{1,5}$"
              }
            ]
          },
          "description": "Initial voter IPs, or IP:controller-port endpoints. Endpoint entries reserve API, gateway, etcd client, and etcd peer ports at P through P+3.",
          "default": []
        },
        "bind-ip": {
          "type": "string",
          "format": "ipv4"
        },
        "bindIp": {
          "$ref": "#/properties/cluster/properties/bind-ip"
        },
        "api-port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 3001,
          "description": "Local controller API port. With IP:port nodes, this selects the local voter entry."
        },
        "apiPort": {
          "$ref": "#/properties/cluster/properties/api-port"
        },
        "gateway-port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 3002,
          "description": "Gateway host port for legacy bare-IP node configuration; IP:port nodes derive this as controller port +1."
        },
        "gatewayPort": {
          "$ref": "#/properties/cluster/properties/gateway-port"
        },
        "control-allow-cidrs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "controlAllowCidrs": {
          "$ref": "#/properties/cluster/properties/control-allow-cidrs"
        },
        "etcd-client-port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 2379,
          "description": "etcd client host port for legacy bare-IP node configuration; IP:port nodes derive this as controller port +2."
        },
        "etcdClientPort": {
          "$ref": "#/properties/cluster/properties/etcd-client-port"
        },
        "etcd-peer-port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 2380,
          "description": "etcd peer host port for legacy bare-IP node configuration; IP:port nodes derive this as controller port +3."
        },
        "etcdPeerPort": {
          "$ref": "#/properties/cluster/properties/etcd-peer-port"
        },
        "shared-registry": {
          "type": "string"
        },
        "sharedRegistry": {
          "$ref": "#/properties/cluster/properties/shared-registry"
        },
        "join-secret": {
          "type": "string",
          "minLength": 32,
          "description": "High-entropy shared secret used to authenticate automatic CA discovery and node joins."
        },
        "joinSecret": {
          "$ref": "#/properties/cluster/properties/join-secret"
        },
        "labels": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        }
      }
    },
    "node": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "role": {
          "type": "string",
          "enum": ["hybrid", "voter", "worker"],
          "default": "hybrid",
          "description": "Hybrid nodes vote and run workloads; voters are control-plane-only; workers run workloads without a local etcd member."
        }
      },
      "default": {}
    },
    "ingress": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "ports": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "default": []
        }
      }
    },
    "subnet": {
      "type": "string"
    },
    "egress": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "deny": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "allow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      }
    },
    "encryption-key": {
      "type": "string"
    },
    "encryptionKey": {
      "$ref": "#/properties/encryption-key"
    },
    "tailscale": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "auth-key": {
          "type": "string"
        },
        "authKey": {
          "$ref": "#/properties/tailscale/properties/auth-key"
        },
        "advertise-routes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "advertiseRoutes": {
          "$ref": "#/properties/tailscale/properties/advertise-routes"
        }
      }
    },
    "jwt-secret-key": {
      "type": "string"
    },
    "jwtSecretKey": {
      "$ref": "#/properties/jwt-secret-key"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "datadog": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "api-key": {
          "type": "string"
        },
        "apiKey": {
          "$ref": "#/properties/datadog/properties/api-key"
        },
        "site": {
          "type": "string"
        },
        "include-ingress-logs": {
          "type": "boolean",
          "default": true
        },
        "includeIngressLogs": {
          "$ref": "#/properties/datadog/properties/include-ingress-logs"
        },
        "include-tailscale-logs": {
          "type": "boolean",
          "default": true
        },
        "includeTailscaleLogs": {
          "$ref": "#/properties/datadog/properties/include-tailscale-logs"
        },
        "logs": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "include-healthcheck": {
              "type": "boolean",
              "default": true
            },
            "includeHealthcheck": {
              "$ref": "#/properties/datadog/properties/logs/properties/include-healthcheck"
            }
          }
        },
        "include-metrics": {
          "type": "boolean",
          "default": false
        },
        "includeMetrics": {
          "$ref": "#/properties/datadog/properties/include-metrics"
        }
      }
    },
    "system": {
      "type": "string",
      "enum": ["nixos"]
    },
    "runtime": {
      "type": "string",
      "enum": ["docker", "nerdctl"],
      "default": "docker"
    },
    "depot": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "token": {
          "type": "string"
        }
      }
    },
    "cloudflare": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "tunnel": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "token": {
              "type": "string"
            },
            "replicas": {
              "type": "integer",
              "minimum": 1
            }
          }
        }
      }
    },
    "slack": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "webhook-url": {
          "type": "string"
        },
        "webhookUrl": {
          "$ref": "#/properties/slack/properties/webhook-url"
        }
      }
    },
    "log-backup": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "bucket": {
          "type": "string",
          "minLength": 1
        },
        "kms-key-id": {
          "type": "string",
          "minLength": 1
        },
        "kmsKeyId": {
          "$ref": "#/properties/log-backup/properties/kms-key-id"
        },
        "region": {
          "type": "string",
          "minLength": 1
        },
        "prefix": {
          "type": "string"
        },
        "retention-days": {
          "type": "integer",
          "minimum": 1
        },
        "retentionDays": {
          "$ref": "#/properties/log-backup/properties/retention-days"
        }
      }
    },
    "logBackup": {
      "$ref": "#/properties/log-backup"
    },
    "disable-etcd-cert": {
      "type": "boolean",
      "default": false
    },
    "disableEtcdCert": {
      "$ref": "#/properties/disable-etcd-cert"
    },
    "allow-cli-deployment": {
      "type": "boolean",
      "default": false
    },
    "allowCliDeployment": {
      "$ref": "#/properties/allow-cli-deployment"
    }
  }
}
